import React, {Fragment} from 'react';
import Route from '../routes';
import Global from '../styles/global';
const App = (props)=>(
  <Fragment>
    <Global/>
    <Route />
  </Fragment>
);

export default App;